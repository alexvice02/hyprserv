# HyprServ service registry
#
#   unit | icon | label | tracked
#
#   unit    systemd unit name, as `systemctl` accepts it
#   icon    Nerd Font glyph shown in the menu
#   label   human-readable name
#   tracked yes|no — whether this unit counts toward the Waybar running/partial/stopped
#           state. Keep this to the services you actually care about at a glance;
#           everything else still appears in the menu.

postgresql       |  | PostgreSQL    | yes
httpd            |  | Apache        | yes
docker           |  | Docker        | yes
php-fpm          |  | PHP-FPM       | no
memcached        | 󰍛 | Memcached     | no
elasticsearch    |  | Elasticsearch | no
nginx            |  | Nginx         | no
redis            |  | Redis         | no
mysql            |  | MySQL         | no
mongod           |  | MongoDB       | no
rabbitmq-server  |  | RabbitMQ      | no
mariadb          |  | MariaDB       | no
